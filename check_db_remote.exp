#!/usr/bin/expect

set timeout 20
set ip "10.20.91.201"
set user "rtrd"
set password "qq123456"

spawn ssh $user@$ip "ls -lh ~/go-strategy-app/data/analysis.db && sqlite3 ~/go-strategy-app/data/analysis.db 'SELECT board_size, COUNT(*) FROM analysis_cache GROUP BY board_size;'"
expect "password:" { send "$password\r" }
expect eof
