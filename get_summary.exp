#!/usr/bin/expect

set timeout 20
set ip "10.20.91.201"
set user "rtrd"
set password "qq123456"

spawn ssh $user@$ip "grep 'Nodes Processed' ~/go-strategy-app/logs/*.log | tail -n 10"
expect "password:" { send "$password\r" }
expect eof
